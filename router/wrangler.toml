name="omniroute-master-proxy"
main="src/index.js"
compatibility_date="2026-09-05"
# آدرس عمومی workers.dev برای راوتر فعال باشد (کلید گیت‌وی آن را محافظت می‌کند)
workers_dev=true
preview_urls=false

[placement]
mode="smart"

[[services]]
binding="BAI_WORKER"
service="omniroute-provider-bai"

[[services]]
binding="DAHL_WORKER"
service="omniroute-provider-dahl"

[vars]
ENVIRONMENT="production"
MAX_BODY_BYTES="26214400"

[secrets]
required=["ROUTER_API_KEY"]
